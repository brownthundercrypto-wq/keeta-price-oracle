# Secrets & runtime state — NEVER commit
.env
.env.*
storage.db
storage.db-journal
*.seed
*.key
*.pem

# Persisted TWAP time-series DB (local runtime state; never commit)
data/
*.sqlite
*.sqlite-*
*.db
*.db-*

# Session continuity notes (may contain throwaway testnet seeds)
PROGRESS.md

# Dependencies
node_modules/

# Logs / OS
*.log
.DS_Store
